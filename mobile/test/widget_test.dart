import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder test – full widget tests require a running ProviderScope
    expect(1 + 1, 2);
  });
}
